# alelele

这是一个使用 Git 和 GitHub 管理的项目。

## 开始使用

将项目文件放入此目录，然后提交并推送：

```bash
git add .
git commit -m "描述本次修改"
git push
```

## 注意事项

- 不要提交密码、访问令牌、API Key 或其他敏感信息。
- 本地环境配置请放入 `.env` 文件；该文件已被 Git 忽略。

